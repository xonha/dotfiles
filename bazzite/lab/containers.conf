# Nested rootless Podman inside Lab. The container has no systemd, so Podman
# manages cgroups directly and writes events and logs to files instead of the
# journal.
[containers]
log_driver = "k8s-file"

[engine]
cgroup_manager = "cgroupfs"
events_logger = "file"
