# Podman engine settings for Docker-compatible workflows.
[engine]
# Use the Docker Compose v2 binary as the `podman compose` provider instead of
# podman-compose: it supports `up --wait`, healthcheck conditions and profiles.
compose_providers = ["/usr/bin/docker-compose"]
# Silence the "executing external compose provider" notice on every call.
compose_warning_logs = false
