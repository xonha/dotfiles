# Resolve short image names (postgres:15, redis:7-alpine) from Docker Hub, as
# Docker does, so existing Compose files and Dockerfiles work unchanged.
unqualified-search-registries = ["docker.io"]
short-name-mode = "permissive"
