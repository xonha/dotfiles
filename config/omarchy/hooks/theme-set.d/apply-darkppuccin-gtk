#!/bin/bash

# Keep the custom GTK theme in sync after Omarchy's GNOME integration runs.
[[ ${1:-} == "darkppuccin" ]] || exit 0

gsettings set org.gnome.desktop.interface gtk-theme "Darkppuccin"
gsettings set org.gnome.desktop.interface color-scheme "prefer-dark"
