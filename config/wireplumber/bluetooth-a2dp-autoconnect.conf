## Auto-connect A2DP playback/capture profiles on Bluetooth devices.
## This keeps high-fidelity playback profiles available after reconnects.

monitor.bluez.rules = [
  {
    matches = [
      {
        device.name = "~bluez_card.*"
      }
    ]
    actions = {
      update-props = {
        bluez5.auto-connect = [ a2dp_sink a2dp_source ]
      }
    }
  }
]
