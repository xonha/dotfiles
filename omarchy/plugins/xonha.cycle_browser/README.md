# Default browser profile

An Omarchy bar widget for `xonha.cycle_browser`. It displays the active
browser profile and cycles it with a left click by running:

```text
~/.config/scripts/cycle-browser-profile.sh --cycle
```

The middle mouse button refreshes the displayed profile. Configure another
script path with the `scriptPath` setting in the bar entry.
